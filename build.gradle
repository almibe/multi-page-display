buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'org.dm.gradle:bundle:0.8.2'
    }
}

apply plugin: 'bundle'
apply plugin: 'java'
apply plugin: 'maven'

sourceCompatibility = '1.8'
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

version = '0.0.1-SNAPSHOT'
group = 'org.almibe'

repositories {
    mavenCentral()
}

dependencies {
}

bundle {
    instruction 'Export-Package', 'org.almibe.controls'
}
